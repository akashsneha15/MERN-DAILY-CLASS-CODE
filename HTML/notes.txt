FrontEnd:
----------
->What are all the user can seen on screen is called as FrontEnd
->to design frontend we have different technologies like [html,css,js,react js, agular js, react js...etc]

backend:
----------
-> backend is the server side programming having api logics, database logics to monitor and manage the data that will comes from frontend(req & res )
-> here also we have some technologies(programming languages) like node js & express, java , pyhton , ruby , c , c++

database
----------
-> database is a place where we can store the data
-> to store the data we have techonology like mysql, sql, postgres sql, mongoDB,newp4j , cassandra, HBase, raike......etc

Web Applications :
------------------
    -> Any Applications that will be run in browsers is called as a Web Applications 
     ->Types of Applications 
        1. Static web Applications
            -> any web applications that doesn't have functionalities is called a static web applications 
            -> To build static web applications we need to use only HTML and css

        2. dynamic web applications
            ->any web applications that  have functionalities is called a static web applications 
            -> To build dynamic web applications we need to use only HTML, CSS and JS
            

Web Page:
---------
    ->HTML documents is called a web pages

WebSite:
--------
    ->Collection of HTML documents is called as a websites

Web server:
-----------
    ->Collection of websites is called as web server

Browser Working Principle
--------------------------
    ->


HTML:
-----
    ->Is used to build structure of the web pages
    ->HTML stands for Hyper text markup languages
    
DOCTYPE html - version of HTML

HTML Tags:
----------
    ->tags are the predefined words which enclosed with both opening and closing angulaer brackets

    ->ex: <h1></h1>,<div></div>,<head></head>


Types of tags:
--------------
    1. Paired tags
            -> any tags having both opening and closing tags
            ex:<html></html>, <head></head>........etc

            i. heading tags

    
            ii. formatting tags
                    ->these tags have thir own CSS behaviors
                    Ex:<b></b>

            iii. other tags